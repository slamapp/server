spring:
  datasource:
    url: ${{ secret.DB-DEV-URL }}
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${{ secret.DB-DEV-USERNAME }}
    password: ${{ secret.DB-DEV-PASSWORD }}

  jpa:
    generate-ddl: true
    database: mysql
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        hbm2ddl:
          query.in_clause_parameter_padding: true
          auto: none

  security:
    oauth2:
      client:
        registration:
          kakao:
            client-name: kakao
            client-id: ${{ secret.KAKAO-CLIENT-ID }}
            client-secret: ${{ secret.KAKAO-CLIENT-SECRET }}
            scope:
              - profile_nickname
              - profile_image
              - account_email
            redirect-uri: ${{ secret.KAKAO-REDIRECT-URI }}
            authorization-grant-type: authorization_code
            client-authentication-method: POST
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

  config:
    import:
      - classpath:aws.yaml
      - classpath:app.properties

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher


jwt:
  header: Authorization
  issuer: ${{ secret.JWT-ISSUER }}
  client-secret: ${{ secret.JWT-CLIENT-SECRET }}
  expiry-seconds: ${{ secret.JWT-EXPIRY-SECONDS }}

server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
