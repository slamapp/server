cloud:
  aws:
    credentials:
      accessKey: ${AWS_ACCESSKEY_PRO}
      secretKey: ${AWS_SECRETKEY_PRO}
    s3:
      bucket: ${AWS_S3_BUCKET_NAME}
    region:
      static: ${AWS_S3_REGION}
    stack:
      auto: false
