cloud:
  aws:
    credentials:
      accessKey: $AWS_ACCESSKEY_PRO}
      secretKey: ${secret.AWS_SECRETKEY_PRO}
    s3:
      bucket: ${secret.AWS_S3_BUCKET_NAME}
    region:
      static: ${secret.AWS_S3_REGION}
    stack:
      auto: false
